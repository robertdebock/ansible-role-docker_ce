---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: docker_ce-ubuntu-devel
    image: ubuntu:devel
provisioner:
  name: ansible
  inventory:
    group_vars:
      all:
        docker_ce_ignore_docker: no
  lint:
    name: ansible-lint
scenario:
  name: ubuntu-devel
